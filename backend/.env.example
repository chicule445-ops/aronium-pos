# Configuración para desarrollo local (SQLite)
# No necesitas DATABASE_URL para usar SQLite localmente.

# Configuración para la NUBE (PostgreSQL)
# Descomenta y utiliza tu URL de Neon.tech o Railway cuando estés listo para desplegar
# DATABASE_URL=postgres://usuario:password@host:port/dbname?sslmode=require

PORT=5000
